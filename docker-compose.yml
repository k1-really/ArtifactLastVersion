version: '3.8'

services:
  app:
    build: .
    container_name: maven-artifact-service
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data
    environment:
      DB_URL: jdbc:sqlite:/app/data/database.db
      MAVEN_REPO_URL: https://repo1.maven.org/maven2
      CACHE_REFRESH_HOURS: 24
      MAVEN_LOCAL_REPO: /app/data/maven-repo